package code
{
	import flash.display.MovieClip;
	import flash.ui.Mouse;

	public class Penta extends MovieClip
	{
		public function Penta()
		{
			this.startDrag(true); // allows Penta to be moved around the screen if it were a curson
		}
	}
}